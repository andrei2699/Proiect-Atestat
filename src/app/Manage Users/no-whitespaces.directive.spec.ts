import { NoWhitespaceDirective } from './no-whitespaces.directive';

describe('NoWhitespacesDirective', () => {
  it('should create an instance', () => {
    const directive = new NoWhitespaceDirective();
    expect(directive).toBeTruthy();
  });
});
