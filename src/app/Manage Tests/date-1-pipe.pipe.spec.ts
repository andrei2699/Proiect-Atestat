import { Date1PipePipe } from './date-1-pipe.pipe';

describe('Date1PipePipe', () => {
  it('create an instance', () => {
    const pipe = new Date1PipePipe();
    expect(pipe).toBeTruthy();
  });
});
